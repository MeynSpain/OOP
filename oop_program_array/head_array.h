#pragma once

namespace kib {
	int* Filling(int* mas, int di, int l);   // Ввод массива
	void Output(int* mas, int l);           // Вывод массива
	int Sum(int* mas, int l);             // Сумма массива
}